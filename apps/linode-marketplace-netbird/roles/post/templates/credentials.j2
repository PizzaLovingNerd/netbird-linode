NetBird URL: https://{{ netbird_fqdn }}
NetBird Admin: Create the first administrator in the browser setup wizard.

Sudo Username: {{ username }}
Sudo Password: {{ post_generated_sudo_password }}

Deployment Directory: {{ netbird_project_dir }}
Compose Logs: cd {{ netbird_project_dir }} && sudo docker compose logs
Compose Health: cd {{ netbird_project_dir }} && sudo docker compose ps
{% if common_reboot_required.stat.exists %}

REBOOT REQUIRED: Confirm SSH access with {{ username }}, then run sudo reboot.
{% endif %}
